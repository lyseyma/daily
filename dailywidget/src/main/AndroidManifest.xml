<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- 
            Legacy widget provider - can be removed if not needed
            This was the original widget implementation using traditional Views
        -->
        <receiver
            android:name=".DailyWidgetProvider"
            android:label="My Widget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/daily_widget_into" />
        </receiver>

        <!--
            Modern Glance-based task widget
            This widget displays tasks from the main app using Jetpack Glance
            Package updated to avoid GMS security issues
        -->
        <receiver
            android:name=".receiver.TaskWidgetReceiver"
            android:label="Daily Task List"
            android:exported="true">

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/daily_widget_into" />
        </receiver>
    </application>
</manifest>
